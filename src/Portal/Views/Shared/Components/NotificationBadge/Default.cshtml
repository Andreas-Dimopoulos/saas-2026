@model NotificationBadgeViewModel

<a class="nav-link text-dark" asp-controller="Notifications" asp-action="Index">
    Notifications
    <span id="notificationBadgeCount" class="badge bg-danger @(Model.UnreadCount == 0 ? "d-none" : "")">@Model.UnreadCount</span>
</a>
